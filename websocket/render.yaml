services:
  - type: web
    name: cansat-relay
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python relay.py --host 0.0.0.0 --path /telemetry --ping 20
    autoDeploy: true