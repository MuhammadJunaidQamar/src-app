import 'package:flutter/material.dart';

class SpatialObjectWidget extends StatelessWidget {
  const SpatialObjectWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('This is the Orientation Screen!');
  }
}
