import 'package:flutter/material.dart';

class LiveGeoLocationWidget extends StatelessWidget {
  const LiveGeoLocationWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('This is the Geo Location Screen!');
  }
}
